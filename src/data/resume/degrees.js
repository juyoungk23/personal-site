import meta from '../meta';

const degrees = [
  {
    school: meta.university.name,
    degree: meta.university.degree,
    link: meta.university.link,
    year: meta.university.graduationYear,
  },
  {
    school: meta.highSchool.name,
    year: meta.highSchool.graduationYear,
  },
];

export default degrees;
